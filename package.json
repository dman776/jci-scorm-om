{
  "name": "scorm-observation-workbook-builder",
  "version": "1.4.0",
  "private": true,
  "description": "Author digital observation workbooks and export them as self-contained SCORM 2004 4th Edition packages for Workday Learning.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node apps/server/index.js",
    "dev": "node apps/server/index.js",
    "build:template": "node scripts/build-template-xlsx.js",
    "export:demo": "node scripts/export-demo.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:e2e": "playwright test tests/e2e"
  },
  "dependencies": {
    "@playwright/test": "^1.63.0",
    "jszip": "3.10.1",
    "nanoid": "5.1.16"
  }
}
